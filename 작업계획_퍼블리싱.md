# HOME SURI HOME 퍼블리싱 작업계획

## 1. 프로젝트 개요
- **앱명**: HOME SURI HOME (AI 셀프 집수리 앱)
- **목표**: 첨부된 디자인 시안 3장(로그인/홈/문제사진등록)을 HTML/CSS/JS로 최대한 동일하게 퍼블리싱
- **뷰포트**: 모바일 우선, max-width 480px, 반응형 아님(모바일 프레임 고정)
- **향후 계획**: React 전환 예정 → 데이터 구조/컴포넌트 단위를 명확히 분리해서 설계

---

## 2. 화면 디자인 분석 요약

### 2-1. 로그인 화면 (login.html)
- 상단 로고 영역: 집 모양 아이콘 + "HOME SURI HOME" 워드마크
- 캐치프레이즈: "사진 한 장으로, 집수리가 쉬워집니다." + 서브텍스트
- 입력 폼: 이메일 인풋(아이콘), 비밀번호 인풋(아이콘 + 보기/숨기기 토글)
- 로그인 버튼(브라운/옐로우 계열 primary), 구분선("간편 로그인")
- Google 소셜 로그인 버튼
- 하단 텍스트 링크: 아이디/비밀번호 찾기, 회원가입
- 최하단: 고객센터 문의 링크
- 하단 고정 탭바(홈/AI스캔/구매/커뮤니티/마이페이지) - 로그인 화면에도 노출되는 디자인이므로 동일 반영

### 2-2. 홈 화면 (index.html)
- 상단 헤더: 로고 + 검색 아이콘 + 알림 아이콘
- 검색 입력창("어떤 수리가 필요하신가요?")
- AI 스캔 프로모션 카드: 배지("NEW AI 솔루션") + 타이틀 + 설명 + "AI 스캔 시작하기" 버튼 + 비버/집 일러스트 배경
- "오늘의 집수리 TOP 3" 리스트 카드(순위 뱃지, 아이콘, 타이틀, 진단 건수, 가이드 링크)
- "계절별 추천 수리" 배너형 카드(이미지 + 배지 + 타이틀/설명 + CTA)
- "최근 진단 기록" 리스트(날짜, 진단내용, 상태 뱃지 - 수리필요/정상 등)
- "인기 수리 가이드" 가로 스크롤 카드(썸네일, 제목, 평점, 조회수)
- "인기 커뮤니티 게시글" 카드(Before/After 이미지, 배지, 제목, 설명, 작성자, 좋아요/댓글 수)
- "나를 위한 AI 추천 수리" 카드(아이콘, 멘트, CTA 버튼)
- 하단 배너 2분할(친환경 수리 / 비용 절감)
- 하단 고정 탭바(5개 메뉴, 홈 활성 상태 스타일 별도)

### 2-3. 문제 사진 등록 화면 (scan.html)
- 바텀시트/모달 형태(둥근 상단 모서리, 상단 핸들바)
- 비버 캐릭터 일러스트 + 카메라 스캔 배지 아이콘
- 타이틀 "사진을 스캔해볼까요?" + 설명 텍스트
- 액션 카드 2개: "직접 촬영하기"(카메라 아이콘), "갤러리에서 선택"(이미지 아이콘)
- 하단 "취소" 버튼(텍스트형)
- (요구사항 추가 반영) 이미지 미리보기 상태 + "분석 시작" 버튼 → 사진 선택 후 전환되는 두번째 상태로 별도 설계 필요

---

## 3. 파일 구조

```
/publishing
├── login.html
├── index.html
├── scan.html
├── /css
│   ├── common.css        # CSS 변수(컬러/폰트/spacing), reset, 공통 컴포넌트(버튼/인풋/탭바/카드 베이스)
│   ├── login.css          # 로그인 화면 전용 스타일
│   ├── home.css            # 홈 화면 전용 스타일
│   └── scan.css            # 문제사진등록 화면 전용 스타일
├── /js
│   ├── common.js           # 공통 유틸(탭바 활성화, 토스트 등), 화면 전환/라우팅 로직
│   ├── data.js              # Mock 데이터 (진단기록, 추천수리, 커뮤니티 게시글, 가이드 등) 배열/객체
│   ├── login.js             # 로그인 화면 인터랙션(비밀번호 토글, 유효성 체크, 로그인 처리)
│   ├── home.js               # 홈 화면 렌더링(data.js 기반 동적 렌더), 인터랙션
│   └── scan.js                # 스캔 모달 인터랙션(촬영/갤러리 선택, 미리보기, 취소/분석시작)
└── /assets
    ├── /images              # 비버 캐릭터, 계절 배너 이미지, 커뮤니티 Before/After 등
    └── /icons                # 아이콘 SVG (검색, 알림, 이메일, 잠금, 카메라, 갤러리 등)
```

---

## 4. 공통 설계 원칙

### 4-1. 화면 전환 방식 (중요 - 확인 필요, 5번 항목 참고)
- 요구사항상 "페이지 이동 없이 JS로 화면 전환(SPA 방식)"과 "html 파일을 화면별로 분리"가 동시에 명시되어 있어 아래와 같이 해석하여 진행 예정:
  - **login.html ↔ index.html**: 로그인은 앱 진입 전 단계이므로 별도 파일 간 이동(location.href) 허용
  - **index.html → scan.html 성격의 화면**: 시안상 scan 화면이 바텀시트/모달 형태이므로, 실제로는 **index.html 내부에 scan 모달 컴포넌트를 포함**하고 JS(common.js)로 show/hide 하는 방식(페이지 이동 없음)으로 구현
  - scan.html은 모달 컴포넌트를 **독립적으로도 미리보기 가능하도록 별도 파일로 유지**(퍼블리싱 검수용) + index.html에 동일 마크업을 JS로 삽입/토글하는 구조로 통일
- 하단 탭바 메뉴 전환도 동일하게 JS 기반 show/hide 방식을 기본으로 하되, 현재 범위는 3개 화면 마크업 구현이 우선이므로 나머지 탭(구매/커뮤니티/마이페이지)은 placeholder만 처리

### 4-2. 디자인 토큰 (common.css에 CSS 변수로 정의)
- **Color**
  - `--color-primary`: #7A5230 (브라운, 텍스트/아이콘 포인트)
  - `--color-primary-light`: #C89B6C 계열 (보조 브라운)
  - `--color-accent`: #F0B23D 계열 (옐로우, CTA 버튼)
  - `--color-bg`: #FDF6ED 계열 (아이보리 배경)
  - `--color-surface`: #FFFFFF (카드 배경)
  - `--color-text-primary`, `--color-text-secondary`, `--color-border`, 상태 뱃지 컬러(위험/정상 등)
- **Typography**: 폰트 패밀리(한글 지원, 예: Pretendard 또는 시스템 폰트 대체), 타이틀/바디/캡션 사이즈 스케일
- **Spacing/Radius**: 8px 기준 spacing scale, 카드 radius(16~20px), 버튼 radius(pill/round)
- **Shadow**: 카드용 soft shadow 1~2단계

### 4-3. 공통 컴포넌트 (common.css/common.js)
- 버튼(primary/secondary/text), 인풋 필드(아이콘 포함), 카드 베이스, 배지(뱃지 컬러 variant), 하단 탭바, 헤더

---

## 5. 확인이 필요한 사항 (계획 승인 시 함께 답변 부탁드립니다)
1. **화면 전환 구조**: 위 4-1 해석(로그인은 별도 페이지 이동 / scan은 index 내 모달)으로 진행해도 될지, 혹은 3개 화면 모두 하나의 진입점(index.html)에서 JS 라우팅으로 처리하길 원하시는지
2. **스캔 화면 2단계**: 시안 이미지에는 사진 선택 전 단계만 있는데, 요구사항의 "이미지 미리보기, 분석 시작" 단계는 별도 상태(사진 선택 후 화면)로 신규 설계가 필요합니다 - 별도 시안이 없으므로 기존 톤앤매너로 자체 구성해도 될지
3. **하단 탭바 서브 메뉴(AI스캔/구매/커뮤니티/마이페이지)**: 이번 범위에서는 클릭 시 실제 화면 없이 탭 활성화 스타일만 구현하고 콘텐츠는 추후 진행으로 이해해도 되는지
4. **폰트**: 시안 폰트가 두꺼운 라운드고딕 계열로 보이는데, 웹폰트 지정(예: Pretendard CDN) 진행해도 되는지, 아니면 시스템 폰트로 대체할지
5. **비버 캐릭터/계절 배너/커뮤니티 이미지**: 실제 리소스 파일이 없으므로 임시 이미지(플레이스홀더 또는 간단한 SVG 일러스트)로 대체 제작 예정 - 추후 실제 리소스 교체 전제로 진행

---

## 6. 데이터 구조 설계 (data.js) - React 전환 대비

```js
// 최근 진단 기록
const diagnosisHistory = [
  {
    id: "diag_001",
    title: "주방 싱크대 수전",
    date: "2023-11-24",
    status: "repair_needed",     // "repair_needed" | "normal"
    statusLabel: "수리 필요",
    description: "본체 하부 미세 누수 확인됨",
  },
];

// 오늘의 집수리 TOP 3
const todayTopRepairs = [
  {
    id: "top_001",
    rank: 1,
    icon: "faucet",
    title: "수전 누수/교체",
    count: 1240,
    countLabel: "오늘 1,240건 진단됨",
    linkType: "guide",
  },
];

// 계절별 추천 수리 배너
const seasonalRecommend = {
  id: "season_winter",
  badge: "WINTER SPECIAL GUIDE",
  title: "동파 예방 & 외풍 차단",
  description: "본격적인 추위가 오기 전 꼭 확인해야 할 보일러 배관 및 창문 틈새 수리 노하우를 확인하세요.",
  image: "/assets/images/season_winter.jpg",
  ctaLabel: "전체 가이드 보기",
  ctaLink: "#",
};

// 인기 수리 가이드
const popularGuides = [
  {
    id: "guide_001",
    thumbnail: "/assets/images/guide_silicon.jpg",
    title: "초보자도 쉬운 실리콘 재시공",
    rating: 4.9,
    viewCount: "조회 2.4k",
  },
];

// 인기 커뮤니티 게시글
const communityPosts = [
  {
    id: "post_001",
    badgeLabel: "인기 리모델링",
    beforeImage: "/assets/images/community_before.jpg",
    afterImage: "/assets/images/community_after.jpg",
    title: "3만원으로 화장실 수납장 살리기",
    description: "생각보다 너무 간단해서 놀랐어요! 페인트랑 손잡이만 바꿨는데 새거 같네요.",
    author: "홀드레실_지니",
    likeCount: 142,
    commentCount: 28,
  },
];

// AI 추천 수리 (개인화)
const aiRecommendation = {
  id: "ai_rec_001",
  icon: "water_drop",
  title: "수압이 약해졌나요?",
  description: "지난주 진단 데이터와 비교했을 때 급수 밸브 점검이 필요해 보입니다.",
  ctaLabel: "자가진단 시작",
};

// 하단 배너
const bottomBanners = [
  { id: "banner_eco", icon: "leaf", label: "친환경 수리" },
  { id: "banner_cost", icon: "coin", label: "비용 절감" },
];

// 로그인 폼 상태 (login.js 내 관리, 구조 예시)
const loginFormState = {
  email: "",
  password: "",
  isPasswordVisible: false,
  errors: { email: null, password: null },
};

// 스캔 상태 (scan.js 내 관리)
const scanState = {
  step: "select",           // "select" | "preview"
  selectedImage: null,       // base64 or objectURL
  source: null,               // "camera" | "gallery"
};
```

---

## 7. 단계별 작업 순서

### 1단계. 공통 스타일 정의
- common.css: reset, CSS 변수(컬러/타이포/spacing/radius/shadow), 공통 버튼/인풋/카드/탭바/헤더 컴포넌트 클래스
- common.js: 탭바 활성화 처리, 모달 open/close 유틸, (필요 시) 간단 라우팅 헬퍼
- data.js: 목데이터 스켈레톤 우선 정의(6번 구조 기반)

### 2단계. 홈 화면 (index.html) 작업
- 마크업: 헤더 → 검색 → AI스캔 프로모션 → TOP3 → 계절추천 → 최근진단기록 → 인기가이드 → 커뮤니티 → AI추천 → 하단배너 → 탭바
- home.css: 섹션별 스타일
- home.js: data.js 기반 동적 렌더링(리스트/카드 반복 생성), AI스캔 버튼 클릭 시 scan 모달 오픈 연결

### 3단계. 로그인 화면 (login.html) 작업
- 마크업: 로고/캐치프레이즈/폼/소셜로그인/하단링크/탭바
- login.css: 인풋/버튼/구분선 스타일
- login.js: 비밀번호 표시 토글, 간단 유효성 검사, 로그인 버튼 클릭 시 index.html 이동 처리(임시)

### 4단계. 문제 사진 등록 화면 (scan.html / 모달) 작업
- 마크업: 바텀시트 컨테이너(select 단계 + preview 단계 2가지 상태)
- scan.css: 모달/바텀시트 레이아웃, 액션 카드 스타일, 미리보기 상태 스타일
- scan.js: 촬영/갤러리 선택 이벤트(input type=file 활용), 이미지 미리보기 렌더, 취소/분석시작 버튼 동작, index.html 모달 연동

### (선택) 5단계. 통합 점검
- 3화면 공통 컴포넌트(탭바/버튼/카드) 일관성 검수
- 반응형(모바일 프레임) 최종 확인
- 코드 정리 및 React 전환 대비 데이터/마크업 구조 재점검

---

## 8. 다음 액션
이 계획에 대해 확인 및 5번 항목 질문에 대한 답변을 주시면, 승인해주신 순서(공통 스타일 → 홈 → 로그인 → 스캔) 그대로 화면별 코드 작업을 순차 진행하겠습니다.

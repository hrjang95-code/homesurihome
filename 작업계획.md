# HOME SURI HOME - React 프로젝트 전환 작업 계획서

기존에 HTML/CSS/JavaScript로 퍼블리싱된 **HOME SURI HOME** 웹 리소스를 React(Vite + TypeScript + styled-components + React Router DOM) 프로젝트로 성공적으로 변환하기 위한 분석 결과 및 단계별 작업 계획을 정의합니다.

---

## 1. 퍼블리싱 원본(`org/`) 폴더 전체 파일 목록 및 화면별 역할

`Publishing/img/org/` 폴더를 재귀적으로 분석한 실제 파일 목록과 각 화면의 역할은 다음과 같습니다.

### 📂 [1_HOME_SURI_HOME_login_screen](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen)
- **[login.html](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/login.html)**: 로그인 화면의 마크업. 이메일/비밀번호 입력 폼과 Google 간편 로그인 버튼이 존재합니다.
- **[login.css](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/login.css)**: 로그인 화면 전용 스타일. (파일 상단에 이전 작업의 대화 기록 주석이 약 1200줄 포함되어 있어, 실제 CSS는 하단부 추출 필요)
- **[login.js](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/login.js)**: 비밀번호 보기/숨기기 토글, 폼 전송 시 빈 입력값 검증, 임시 로그인 완료 토스트 출력 후 홈 화면(`../index.html`)으로 이동하는 인터랙션 담당.
- **[common.css](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/common.css)**: 폰트페이스 정의, CSS 테마 변수, 리셋 스타일, 전체 모바일 프레임(`app-shell`), 하단 탭바(`bottom-nav`), 토스트(`toast`) 등 공통 스타일.
- **[common.js](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/common.js)**: Lucide 아이콘 렌더링 초기화 및 토스트 노출 공통 함수.
- **[README.md](file:///d:/장혜리/project001/Publishing/img/org/1_HOME_SURI_HOME_login_screen/README.md)**: 로그인 성공 후 이동 경로 안내 문서.

### 📂 [3_HOME_SURI_HOME_result_screen](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen)
- **[result.html](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/result.html)**: AI 문제 진단 결과 요약 화면 마크업. 난이도/소요시간/비용 요약 그리드, 주의사항 카드, 공구 및 부품 카드, 단계별 가이드 카드, 최종 체크리스트로 구성됩니다.
- **[result.css](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/result.css)**: 결과 화면 전용 스타일.
- **[result.js](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/result.js)**: 진단 데이터를 읽어와 각 요소를 동적으로 화면에 렌더링하고, 체크리스트 전체 동의 여부에 따른 수리 완료 토스트 출력 및 전문가 연결 링크 인터랙션 구현.
- **[data.js](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/data.js)**: `diagnosisResult` (현관문 도어클로저 누유 감지 결과) 모의 데이터 선언.
- **[common.css](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/common.css)** & **[common.js](file:///d:/장혜리/project001/Publishing/img/org/3_HOME_SURI_HOME_result_screen/common.js)**: 상동.

### 📂 [4_HOME_SURI_HOME_shop_screen](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen)
- **[shop.html](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/shop.html)**: 수리용 추천 공구 안내 및 구매 화면 마크업. AI 추천 메인 제품, 추천 이유 카드, 온라인 최저가 비교 리스트, 비슷한 추천 상품 가로 스크롤 카드, 주변 철물점 목록 및 지도, 비버 꿀팁 카드로 구성됩니다.
- **[shop.css](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/shop.css)**: 상점 화면 전용 스타일.
- **[shop.js](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/shop.js)**: 상점 데이터를 동적 렌더링하고, 찜하기(하트) 토글 인터랙션, 온라인 구매/철물점 문의 토스트 노출, 주변 철물점 위치로 부드러운 스크롤 이동 인터랙션 구현.
- **[data.js](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/data.js)**: `toolShop` (드릴 세트 상품 정보, 최저가 비교, 철물점 등) 모의 데이터 선언.
- **[common.css](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/common.css)** & **[common.js](file:///d:/장혜리/project001/Publishing/img/org/4_HOME_SURI_HOME_shop_screen/common.js)**: 상동.

### 📂 [6_HOME_SURI_HOME_expert_screen](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen)
- **[expert.html](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/expert.html)**: 수리 전문가 찾기 화면 마크업. 검색 필드, 카테고리 탭 리스트, AI 추천 전문가 요약(SOS 카드), 전문가 정보 리스트로 구성됩니다.
- **[expert.css](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/expert.css)**: 전문가 화면 전용 스타일.
- **[expert.js](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/expert.js)**: 카테고리 탭 선택 및 검색어 인풋 필터링을 통해 목록을 동적으로 리렌더링하고 견적 문의, 전화 연락, 예약하기 버튼 클릭 시의 토스트 작동 및 뒤로가기 버튼 연동 처리.
- **[data.js](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/data.js)**: `expertCategories` 및 `experts` 모의 데이터 선언.
- **[common.css](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/common.css)** & **[common.js](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/common.js)**: 상동.
- **[README.md](file:///d:/장혜리/project001/Publishing/img/org/6_HOME_SURI_HOME_expert_screen/README.md)**: 전문가 찾기 화면 설명 및 컴포넌트 병합 가이드라인 문서.

---

## 2. HTML/CSS/JavaScript 간 연결 관계

- **HTML ↔ CSS**: 각 페이지 HTML 마크업 파일은 `<head>` 내에 공통 리셋/레이아웃을 갖는 `common.css`를 먼저 불러온 후, 화면 전용 CSS 스타일(`login.css`, `result.css`, `shop.css`, `expert.css`)을 순서대로 임포트하여 스타일을 결합시킵니다.
- **HTML ↔ JS (데이터 바인딩)**: 바디 최하단에서 CDN 형식으로 Lucide 아이콘 라이브러리를 로드한 후, 데이터 파일 `data.js`와 공통 스크립트 `common.js`, 개별 비즈니스 로직 `*.js`를 순차 실행합니다.
- **데이터 공급 및 조작**: 개별 `*.js` 파일은 `DOMContentLoaded` 시점에 전역 객체인 `window.HOME_SURI_DATA`의 속성값들을 탐색하여 마크업 상의 ID 선택자(`.querySelector`)에 매칭시키고, `innerHTML` 또는 `textContent`를 통해 데이터를 동적으로 주입합니다.
- **인터랙션 및 공통 함수 사용**: 사용자의 이벤트(클릭, 키 입력 등) 발생 시 `common.js`에 설정되어 있는 전역 `window.HOME_SURI_COMMON` 객체의 `toast()` 알림 및 `initIcons()` 아이콘 렌더링 호출을 실행하여 일관된 팝업 피드백을 전달합니다.

---

## 3. 공통 코드와 화면 전용 코드 구분

### 3-1. 공통 코드 (Common Core)
- **디자인 토큰 (common.css 변수)**: 포인트 브라운 컬러(`--brown-900`, `--brown-700`, `--brown-600`), 크림색 배경(`--cream`), 백그라운드(`--white`), 텍스트 및 뮤트 색상, 그림자 효과(`--shadow`, `--shadow-soft`).
- **전역 초기화 (common.css reset)**: 한나체 Air 폰트 페이스 설정, 전체 박스 사이징 초기화, 모바일 전체 프레임 레이아웃 스타일(`.app-shell`).
- **공통 네비게이션 및 헤더**: 하단 고정 탭 메뉴(`.bottom-nav`, `.nav-item`, `.nav-item.active`), 상단 공통 헤더(`.topbar`, `.brand`).
- **전역 인터랙션 유틸 (common.js)**: Lucide 아이콘 생성(`initIcons`), 토스트 팝업(`.toast`, `toast(message)`).

### 3-2. 화면 전용 코드 (Page Specifics)
- **로그인**: 로그인 화면 전용 스타일 및 폼 유효성 체크, 비밀번호 표시 전환 토글 및 로그인 처리 후 임시 라우팅.
- **결과**: `diagnosisResult` 객체 데이터 구성, 단계별 가이드 렌더링 및 완료 체크리스트 조건문 검사.
- **상점**: `toolShop` 객체 데이터 구성, 최저가 목록 렌더링, 찜 목록 하트 토글 처리 및 철물점 지도 컴포넌트 스크롤 제어.
- **전문가**: `expertCategories` 및 `experts` 데이터 구성, 검색 및 카테고리 탭 선택 상태에 따른 전문가 리스트 필터링 기능.

---

## 4. React 페이지 및 라우터 경로 설계

기존 화면의 라우터 경로 및 미구현/추후 구현 예정인 페이지들을 정리합니다.

### 4-1. React 페이지 컴포넌트 목록
1. **LoginPage (`src/pages/LoginPage.tsx`)**
   - 기존의 `login.html`과 1대1 매핑. 공통 헤더 없이 단독 로그인 폼과 브랜딩 요소를 렌더링합니다.
2. **ResultPage (`src/pages/ResultPage.tsx`)**
   - 기존의 `result.html`과 1대1 매핑. AI 진단 결과 정보와 가이드를 렌더링합니다.
3. **ShopPage (`src/pages/ShopPage.tsx`)**
   - 기존의 `shop.html`과 1대1 매핑. 추천 전동 드릴 정보 및 철물점 지도를 표시합니다.
4. **ExpertPage (`src/pages/ExpertPage.tsx`)**
   - 기존의 `expert.html`과 1대1 매핑. 수리 전문가 검색 및 카테고리별 탐색 리스트를 표시합니다.
5. **HomePage (`src/pages/HomePage.tsx`)** - *[추후 구현]*
   - 기존 퍼블리싱 1단계에 있었던 `index.html` 기반의 홈 화면.
6. **ScanPage (`src/pages/ScanPage.tsx`)** - *[추후 구현]*
   - AI 이미지 분석을 시작하는 문제 사진 등록 화면.

### 4-2. React Router 경로 설계 (`src/App.tsx`)

| URL 경로 | 매핑 페이지 Component | 활성 탭 상태 | 비고 / 인터랙션 흐름 |
| :--- | :--- | :--- | :--- |
| `/login` | `LoginPage` | (없음) | 최초 진입 또는 로그아웃 시 활성화 |
| `/` 또는 `/home` | `HomePage` (임시 대처) | 홈 | 추후 구현 예정 (현재는 "준비중" 혹은 임시 홈 화면) |
| `/scan` | `ScanPage` (임시 대처) | AI 스캔 | 추후 구현 예정 |
| `/result` | `ResultPage` | AI 스캔 (또는 AI 진단) | 수리 완료 및 전문가 연결 등으로 분기 가능 |
| `/shop` | `ShopPage` | 상점 | 추천 상품 및 지도 링크 제공 |
| `/expert` | `ExpertPage` | 전문가 | 카테고리 필터링 및 검색, 뒤로가기 가능 |
| `/community` | `CommunityPage` (임시) | 커뮤니티 | 추후 구현 예정 (현재는 Toast 피드백 처리) |
| `/mypage` | `MyPage` (임시) | 마이페이지 | 추후 구현 예정 (현재는 Toast 피드백 처리) |

---

## 5. 공통 컴포넌트 설계

반복되어 재사용성이 높은 UI 요소를 공통 컴포넌트로 분류하여 설계합니다.

- **`AppLayout` (`src/components/AppLayout.tsx`)**
  - 모바일 해상도 프레임(`width: min(100%, 390px)`)을 감싸는 전체 셸 레이아웃.
- **`Header` (`src/components/Header.tsx`)**
  - 상단 브랜딩 텍스트/로고 및 검색, 알림 아이콘 버튼 영역. (로그인 페이지를 제외한 모든 페이지 공용)
  - 뒤로가기 화살표 버튼이 있는 서브 페이지 헤더 디자인 분기 처리(전문가 페이지 등).
- **`BottomNavigation` (`src/components/BottomNavigation.tsx`)**
  - 홈, AI 스캔, 상점, 커뮤니티, 마이페이지 이동 탭바.
  - 현재 활성화된 탭에 맞춰 원형의 강조 스타일 및 브라운 계열 배경 아이콘 렌더링.
- **`Button` (`src/components/Button.tsx`)**
  - `PrimaryButton` (옐로우/골드 계열의 주요 버튼)
  - `OutlineButton` (외곽선만 있는 서브 버튼)
  - `IconButton` (검색, 알림, 하트, 닫기 등 아이콘 단독 버튼)
- **`Card` (`src/components/Card.tsx`)**
  - 수리 요약 정보, 최저가 판매처, 철물점 항목 등 둥근 모서리와 soft shadow를 품는 공통 카드 컨테이너.
- **`StatusBadge` (`src/components/StatusBadge.tsx`)**
  - 신뢰도 배지, 재고 상태 표시 배지, 피스 규격 명칭 등 다양한 톤의 인라인 태그 뱃지.
- **`Toast` (`src/components/Toast.tsx`)**
  - 하단에 떠올랐다가 사라지는 알림 레이어. 전역 Context 또는 Portal 형태로 작동하도록 설계.

---

## 6. 데이터 구조 및 TypeScript 타입 설계

`data.js`를 구조화된 TypeScript 데이터 파일로 전환하기 위한 타입 정의 설계안입니다.

### 6-1. TypeScript 타입 선언 (`src/types/*.ts`)

#### [NEW] [result.ts](file:///d:/장혜리/project001/src/types/result.ts)
```typescript
export interface SummaryItem {
  icon: string;
  label: string;
  value: string;
}

export interface ToolItem {
  icon: string;
  label: string;
}

export interface PartInfo {
  image: string;
  badge: string;
  name: string;
  description: string;
  note: string;
}

export interface StepItem {
  id: string;
  image: string;
  title: string;
  description: string;
}

export interface DiagnosisResult {
  issueTitle: string;
  confidence: number;
  summary: SummaryItem[];
  cautions: string[];
  requiredTools: ToolItem[];
  part: PartInfo;
  steps: StepItem[];
  checklist: string[];
}
```

#### [NEW] [shop.ts](file:///d:/장혜리/project001/src/types/shop.ts)
```typescript
export interface StockLabel {
  label: string;
  tone: string; // 'blue' | 'green' | ''
}

export interface FeaturedProduct {
  id: string;
  badge: string;
  tag: string;
  image: string;
  name: string;
  rating: number;
  reviewCount: number;
  soldCount: number;
  price: number;
  stockLabels: StockLabel[];
}

export interface RecommendReason {
  image: string;
  title: string;
  tag: string;
  badge: string;
  text: string;
}

export interface PriceComparison {
  id: string;
  logo: string;
  storeName: string;
  price: number;
  note: string;
  isLowest: boolean;
}

export interface SimilarProduct {
  id: string;
  image: string;
  name: string;
  price: number;
  rating: number;
}

export interface StoreInfo {
  id: string;
  name: string;
  distance: string;
  closeTime: string;
  stockStatus: string;
}

export interface NearbyStores {
  mapImage: string;
  locationLabel: string;
  stores: StoreInfo[];
}

export interface TipInfo {
  badge: string;
  tag: string;
  image: string;
  text: string;
}

export interface ToolShop {
  intro: {
    title: string;
    description: string;
    image: string;
  };
  featuredProduct: FeaturedProduct;
  recommendReason: RecommendReason;
  priceComparison: PriceComparison[];
  similarProducts: SimilarProduct[];
  nearbyStores: NearbyStores;
  tip: TipInfo;
}
```

#### [NEW] [expert.ts](file:///d:/장혜리/project001/src/types/expert.ts)
```typescript
export interface ExpertCategory {
  id: string;
  label: string;
}

export interface ExpertInfo {
  id: string;
  category: string;
  name: string;
  image: string;
  match: number;
  specialty: string;
  rating: number;
  reviewCount: number;
  experience: string;
  tags: string;
  region: string;
  price: string;
  arrival: string;
  review: string;
}
```

### 6-2. data.js 변환 계획 (`src/data/*.ts`)
각 화면 데이터는 타입 안전성을 보장받을 수 있도록 상기 선언된 타입을 부여하여 개별 Export 파일로 구성합니다.

- **`src/data/resultData.ts`**: `DiagnosisResult` 타입을 기반으로 `diagnosisResult` 객체 데이터를 정의하고 내보냅니다.
- **`src/data/shopData.ts`**: `ToolShop` 타입을 기반으로 `toolShop` 객체 데이터를 정의하고 내보냅니다.
- **`src/data/expertData.ts`**: `ExpertCategory[]` 및 `ExpertInfo[]` 타입을 기반으로 각각 `expertCategories`와 `experts` 데이터를 정의하고 내보냅니다.

---

## 7. styled-components 변환 계획

CSS 변수를 theme 객체로 안전하게 이전하고, 공통 스타일과 화면별 컴포넌트 스타일을 격리화하여 작성합니다.

### 7-1. 공통 테마 설정 (`src/styles/theme.ts`)
```typescript
import { DefaultTheme } from 'styled-components';

export const theme: DefaultTheme = {
  colors: {
    primary: '#7A5230',       // 브라운 700
    primaryDark: '#4F2D1B',   // 브라운 900
    primaryLight: '#98623C',  // 브라운 600
    accent: '#F7BD4C',        // 옐로우/골드
    cream: '#FBF7F2',         // 아이보리 배경
    white: '#FFFFFF',
    text: '#2B211C',          // 다크 브라운 텍스트
    muted: '#8E7E73',         // 회갈색 텍스트
    divider: '#DFD4CB',
    toastBg: '#3C2B20',
  },
  shadows: {
    default: '0 14px 32px rgba(106, 72, 45, 0.12)',
    soft: '0 8px 20px rgba(106, 72, 45, 0.08)',
    bottomNav: '0 -10px 28px rgba(91, 56, 34, 0.10)',
    shell: '0 0 36px rgba(84, 57, 39, 0.13)',
  },
  borderRadius: {
    card: '20px',
    button: '15px',
    pill: '999px',
  }
};
```

### 7-2. GlobalStyle 설정 (`src/styles/GlobalStyle.ts`)
- 전역 리셋과 `@font-face`를 통한 한나체 Air 폰트 경로 등록.
- `body` 태그의 배경색을 회색 조로 구성하여 셸이 가운데 정렬될 수 있는 구조 유지.

```typescript
import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`
  @font-face {
    font-family: 'HannaAir';
    src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_four@1.0/BMHANNAAir.woff') format('woff');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  * {
    box-sizing: border-box;
  }

  html, body {
    margin: 0;
    padding: 0;
    min-height: 100%;
    background-color: #EDE8E3;
    color: ${({ theme }) => theme.colors.text};
    font-family: 'HannaAir', sans-serif;
  }

  body {
    display: flex;
    justify-content: center;
  }

  button, input, a {
    font: inherit;
  }

  button {
    padding: 0;
    border: 0;
    cursor: pointer;
    background: transparent;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  img {
    display: block;
    max-width: 100%;
    height: auto;
  }
`;
```

---

## 8. 이미지 파일 이동 및 이름 변경 계획

기존 번호 위주로 관리되던 모호한 이미지 파일명을 용도와 화면에 맞춰 의미 있는 이름으로 매핑하여 복사 및 정리합니다.

- **복사 전 원본 위치**: `Publishing/img/`
- **복사 후 React 대상 위치**: `src/assets/images/`

### 📋 이미지 파일명 매핑 및 용도 설명
| 원본 파일명 | React 복사 파일명 | 용도 및 화면 설명 |
| :--- | :--- | :--- |
| `3_004.png` | `logo-brand.png` | 로그인 화면 등 최상단 메인 브랜드 워드마크 로고 |
| `2_004.png` | `logo-google.png` | 간편 로그인 버튼 내 Google 'G' 아이콘 |
| `1_004.png` | `beaver-login.png` | 로그인 페이지용 망치 들고 집 수리하는 비버 일러스트 |
| `1_002.png` | `beaver-hammer.png` | 결과화면 히어로 영역 및 상점화면 팁에 사용되는 공통 망치 비버 |
| `1_003.png` | `beaver-shop-intro.png` | 상점 상단 소개 영역의 공구를 안고 있는 비버 일러스트 |
| `3_003.png` | `beaver-recommend-reason.png` | 상점 화면 '비버의 추천 이유' 카드에 들어가는 비버 일러스트 |
| `1_005.png` | `beaver-expert-sos.png` | 전문가 찾기 SOS 섹션의 안전모 쓰고 인사하는 비버 일러스트 |
| `2_002.png` | `part-screw-m5-15.png` | 결과 화면 필요 부품(M5 x 15mm 피스 나사) 제품 사진 |
| `3_002.png` | `step-01-disassemble.png` | 결과 화면 가이드 STEP 1 (기존 본체 탈거) 사진 |
| `4_002.png` | `step-02-assemble.png` | 결과 화면 가이드 STEP 2 (새 본체 장착 및 조절) 사진 |
| `2_003.png` | `product-featured-drill.png` | 상점 화면 메인 추천 다용도 전동 드릴 세트 제품 사진 |
| `4_003.png` | `product-bosch-drill.png` | 상점 화면 하단 비슷한 추천 상품 - 보쉬 충전 드릴 제품 사진 |
| `5_003.png` | `product-makita-drill.png` | 상점 화면 하단 비슷한 추천 상품 - 마끼다 해머 드릴 제품 사진 |
| `6_003.png` | `product-dewalt-drill.png` | 상점 화면 하단 비슷한 추천 상품 - 디월트 임팩트 드릴 제품 사진 |
| `8_003.png` | `logo-coupang.png` | 최저가 쇼핑몰 비교 - 쿠팡 기업 로고 |
| `10_003.png` | `logo-naver.png` | 최저가 쇼핑몰 비교 - 네이버 쇼핑 로고 |
| `9_003.png` | `logo-ohouse.png` | 최저가 쇼핑몰 비교 - 오늘의집 로고 |
| `7_003.png` | `map-stores.png` | 주변 철물점 위치를 시각화한 정적 지도 이미지 |
| `2_005.png` | `profile-expert-kim.png` | AI 추천 전문가 리스트 - 김수리 반장님 프로필 사진 |
| `3_005.png` | `profile-expert-lee.png` | AI 추천 전문가 리스트 - 이지안 명장 프로필 사진 |
| `part-screw.png` | `part-screw-detail.png` | 나사 자재 관련 상세 컷 이미지 |

*참고: `1_001.png`, `3_001.png`, `4_001.png`, `5_001.png`, `6_001.png`, `7_001.png`, `8_001.png`, `9_001.png`는 원본 마크업이나 스타일시트 어디에도 바인딩되어 있지 않으며 피그마 기획안의 캡처본(시안 이미지)이므로 백업용 보존 대상 폴더로 격리 처리하고 React 빌드 자산에는 포함하지 않습니다.*

---

## 9. 기존 JavaScript 인터랙션 전환 계획

### 9-1. 로그인 화면 (`LoginPage.tsx`)
- **비밀번호 보기/숨기기**:
  - `const [showPassword, setShowPassword] = useState(false)` 선언.
  - 버튼 클릭 시 상태 반전(`!showPassword`) 처리하고, input의 `type` 속성을 `password` 또는 `text`로 조건부 바인딩.
  - 아이콘은 `lucide-react`의 `<EyeOff>` 혹은 `<Eye>` 컴포넌트로 렌더링.
- **로그인 시도 및 유효성 검사**:
  - `email`과 `password` 입력값을 `useState`로 바인딩하여 제어 컴포넌트로 관리.
  - Submit 핸들러에서 공백 필드 발견 시 focus를 유도하고 Toast 호출.
  - 조건 충족 시 Toast 알림 노출 후 `setTimeout`을 통해 `useNavigate()` 메서드로 다음 페이지(/result) 이동 처리.

### 9-2. 분석 결과 화면 (`ResultPage.tsx`)
- **체크리스트 상태 검사**:
  - `const [checklistChecked, setChecklistChecked] = useState<boolean[]>([false, false, false])` 선언.
  - 체크박스 클릭 시 인덱스별 값 토글.
- **수리 완료 버튼**:
  - 버튼 클릭 시 `checklistChecked.every(Boolean)`이 참일 경우 "수리 완료로 기록했어요!" Toast를 띄우고, 거짓일 경우 "체크리스트를 모두 확인해주세요." Toast 노출.

### 9-3. 공구 추천 및 구매 화면 (`ShopPage.tsx`)
- **찜하기 토글**:
  - `const [isLiked, setIsLiked] = useState(false)` 선언.
  - 하트 아이콘 클릭 시 `isLiked` 상태 반전 및 `.liked` 클래스 유무에 맞춰 "찜 목록에 저장했어요." 혹은 "찜을 해제했어요." Toast 출력.
- **주변 철물점 이동**:
  - 주변 철물점 구매 버튼 클릭 시 `useRef`를 지정한 주변 철물점 `<section>` 요소를 타겟으로 `element.scrollIntoView({ behavior: 'smooth' })` 실행.

### 9-4. 전문가 찾기 화면 (`ExpertPage.tsx`)
- **카테고리 탭 탐색 및 실시간 검색 필터링**:
  - `const [selectedCategory, setSelectedCategory] = useState('all')` 및 `const [searchQuery, setSearchQuery] = useState('')` 선언.
  - 카테고리 버튼 클릭 시 `selectedCategory` 변경.
  - 인풋 필드 입력 시 `searchQuery` 상태 갱신.
  - `useMemo`를 이용하여 두 상태가 갱신될 때마다 `experts` 배열을 필터링하여 렌더링 리스트 자동 갱신.
- **뒤로가기 버튼**:
  - `useNavigate(-1)`를 호출하여 브라우저 히스토리 기반 이전 화면 이동. 히스토리가 존재하지 않을 시 안전하게 Toast로 알림.

---

## 10. 원본과 React 화면 비교 검수 방법

1. **디바이스 에뮬레이터 셋업**: Chrome/Edge 브라우저의 개발자 도구(F12) 디바이스 툴바를 활성화하고, 타겟 크기인 **390px (iPhone 12/13/14 Pro)** 및 **430px (iPhone 14/15 Pro Max)** 뷰포트 환경을 고정합니다.
2. **동시 화면 비교 검수 (Side-by-Side)**:
   - 좌측 화면: 기존 퍼블리싱 원본 로컬 파일 실행 (예: `http://127.0.0.1:5500/Publishing/img/org/1_HOME_SURI_HOME_login_screen/login.html`)
   - 우측 화면: React 로컬 개발 서버 실행 (예: `http://localhost:5173/login`)
3. **스타일 정확도 분석**:
   - `HannaAir` 폰트의 볼드 두께 및 줄 바꿈 간격 일치 여부 확인.
   - 각 카드형 UI 컴포넌트의 `border-radius(20px)`와 브라운 그림자 투명도(`rgba(106, 72, 45, 0.12)`)의 시각적 일치 여부 대조.
   - Lucide-react 아이콘의 stroke-width 크기가 기존 UMD 라이브러리와 시각적으로 크기 차이가 없는지 픽셀 단위 체크.
4. **반응형 컨테이너 제한 검수**: 브라우저 창 가로 길이를 넓혔을 때, 양측 모두 중앙 정렬이 되면서 `max-width: 390px` 이내의 크기로 컨텐츠 영역이 흐트러짐 없이 유지되는지 확인합니다.

---

## 11. 예상 오류 및 대응 방법

- **⚠️ 문제점: `lucide-react`와 UMD 로더 간 아이콘 키값 불일치**
  - *원인*: UMD 방식에서 사용한 `data-lucide="brush-cleaning"`이나 `screwdriver` 같은 속성이 React 패키지(`lucide-react`)에서는 다른 네이밍(예: `Brush` 혹은 `Screwdriver` 컴포넌트)을 사용함.
  - *대응*: 원본 HTML에서 사용된 전체 아이콘 목록을 스캔하여 아래 매핑 테이블에 맞춰 컴포넌트를 직접 매핑 처리합니다.
    - `house` → `<House size={21} />`
    - `mail` → `<Mail />`
    - `lock-keyhole` → `<LockKeyhole />`
    - `eye-off` / `eye` → `<EyeOff />` / `<Eye />`
    - `search` → `<Search />`
    - `bell` → `<Bell />`
    - `wrench` → `<Wrench />`
    - `compass` → `<Compass />`
    - `hammer` → `<Hammer />`
    - `brush-cleaning` → `<Brush />` (또는 적절한 청소도구 아이콘 대체)
    - `triangle-alert` → `<TriangleAlert />`
    - `list-checks` → `<ListChecks />`
    - `users` → `<Users />`
    - `circle-check` → `<CircleCheck />`
    - `shopping-cart` / `shopping-bag` → `<ShoppingCart />` / `<ShoppingBag />`
    - `user-round` → `<UserRound />`
    - `star` → `<Star />`
    - `store` → `<Store />`
    - `phone` → `<Phone />`
    - `navigation` → `<Navigation />`
    - `arrow-left` → `<ArrowLeft />`
    - `chevron-down` → `<ChevronDown />`
    - `badge-check` → `<BadgeCheck />`
    - `list-pen` → `<ListPen />`
    - `calendar-days` → `<CalendarDays />`

- **⚠️ 문제점: styled-components 테마 타입 미정의 컴파일 에러**
  - *원인*: TypeScript 환경에서 styled-components의 `DefaultTheme` 구조를 확장 등록하지 않고 `theme.colors` 등을 참조할 시 컴파일 오류가 납니다.
  - *대응*: `src/styles/styled-components.d.ts` 선언 파일을 생성하고, `DefaultTheme` 인터페이스를 선언하여 theme 구성 요소를 강력한 타입 구조로 바인딩합니다.

- **⚠️ 문제점: SPA 라우팅 시 스크롤 위치 보존 이슈**
  - *원인*: React Router를 이용해 화면 전환 시, 스크롤 높이가 이전 페이지 상태로 유지되는 고질적 버그가 발생합니다.
  - *대응*: 라우트 변경을 감지하여 윈도우 스크롤을 최상단으로 리셋해 주는 `<ScrollToTop />` 유틸 컴포넌트를 작성하여 `App.tsx` 최상위에 래핑합니다.

---

## 12. 단계별 작업 순서 및 완료 조건

### 1단계: 원본 전체 분석
- **내용**: `org/` 폴더 내 HTML/CSS/JS 및 이미지 리소스를 재귀적으로 읽어 요구사항 및 디자인 스펙 최종 확정.
- **완료 조건**: 페이지 4개(로그인, 결과, 상점, 전문가)의 마크업 및 데이터 의존성 맵 완비.

### 2단계: Vite + React + TypeScript 프로젝트 구조 설계
- **내용**: 프로젝트 폴더에서 React 구성을 위한 기본 개발 패키지 구성 및 디렉토리 스켈레톤 설계.
- **완료 조건**: 빌드 셋업 파일 구성 완료 및 빈 폴더 구조 구축.

### 3단계: 공통 테마 및 GlobalStyle 설계
- **내용**: `common.css`의 디자인 토큰을 styled-components `theme.ts`와 `GlobalStyle.ts`로 이관.
- **완료 조건**: `@font-face` 한나체 Air 연결 확인 및 body 기본 배경색 중앙 정렬 구현 완료.

### 4단계: 공통 레이아웃과 공통 컴포넌트 설계
- **내용**: `AppLayout`, `Header`, `BottomNavigation`, `Toast`, `Button` 등 공용 레이아웃 및 범용 컴포넌트 마크업/스타일링 분리.
- **완료 조건**: 개별 화면 뼈대 없이 공통 컴포넌트들의 정상 렌더링 확인.

### 5단계: 페이지별 React 컴포넌트 전환
- **내용**: 로그인, 결과, 상점, 전문가 HTML 요소를 styled-components 스타일링과 함께 React 컴포넌트 파일로 분할 변환.
- **완료 조건**: 각 컴포넌트의 정적 마크업과 전용 스타일이 화면에 온전히 그려짐.

### 6단계: data.js와 TypeScript 타입 전환
- **내용**: 데이터 및 카테고리 정보 객체를 TypeScript interface와 함께 개별 TS 파일로 추출 및 이관.
- **완료 조건**: 컴포넌트 내 데이터 바인딩 시 타입 컴파일 에러 제거 완료.

### 7단계: 라우터 및 화면 연결
- **내용**: `react-router-dom` 라이브러리를 이용하여 `App.tsx` 내에 경로를 설정하고 하단 네비게이션 탭 동작과 페이지 이동 구성.
- **완료 조건**: 브라우저 주소 표시줄 경로 입력 및 탭 메뉴 클릭 시 적절한 페이지 컴포넌트로 부드럽게 라우팅됨.

### 8단계: 기존 JavaScript 인터랙션 전환
- **내용**: 비밀번호 토글, 찜하기, 수리 체크리스트, 검색 필터링 등 순수 JS로 제어하던 DOM 조작을 React의 `useState` 상태 기반으로 재구성.
- **완료 조건**: querySelector가 전면 배제되고 React 내부 상태에 맞춰 UI 반응형 제어 정상 작동.

### 9단계: 이미지와 아이콘 리소스 정리
- **내용**: 이미지들을 의미 있는 이름으로 매핑 복사하여 assets 폴더에 넣고, Lucide CDN 호출 제거 후 `lucide-react` 모듈로 교체.
- **완료 조건**: 로컬 이미지 및 모든 아이콘 에셋이 깨짐 없이 정상 출력됨.

### 10단계: 원본 디자인 비교 및 반응형 검수
- **내용**: 기기의 크기를 390px, 430px로 교차 설정하고 원본 브라우저 화면과의 Side-by-Side 픽셀 단위 일치 검수 진행.
- **완료 조건**: 두 화면의 레이아웃 배치가 눈대중 및 실제 치수상으로 98% 이상 동일하게 일치함.

### 11단계: 오류 수정 및 최종 정리
- **내용**: TypeScript 빌드 테스트(`npm run build`)를 수행하여 미사용 변수나 타입 에러를 수정하고 최종 소스 정리.
- **완료 조건**: 에러 없는 완벽한 빌드 번들 생성 완료.

---

## 13. 수정하지 말아야 할 원본 및 안전성 보존 규칙

- ⚠️ **디자인 불변의 법칙**: 기존 브라운 계열 포인트 컬러, 아이보리 배경, 한나체 Air의 고유 폰트 패밀리, 카드 UI 디자인을 임의로 재디자인하거나 변경하지 않습니다.
- ⚠️ **비버 캐릭터 보존**: 비버 캐릭터 이미지를 다른 임의의 캐릭터로 교체하거나 삭제하지 않습니다.
- ⚠️ **모바일 프레임 규격**: 데스크톱 환경에서도 가로 뷰포트가 제한되도록 `.app-shell`의 `width: min(100%, 390px)` 조건을 엄격히 유지합니다.
- ⚠️ **파일 독립성**: 작업 대상인 `Publishing/img/org` 폴더의 HTML, CSS, JS 원본 파일은 변환 중이나 변환 완료 후에도 절대 삭제하거나 수정하지 않고 그대로 보존합니다.

---

## 14. 최종 React 프로젝트 예상 폴더 구조

```
D:/장혜리/project001
├── Publishing/                     # (보존) 원본 퍼블리싱 폴더
│   └── img/
│       ├── org/
│       └── ...
├── src/
│   ├── assets/
│   │   └── images/                 # 이름이 정돈된 이미지 자산
│   │       ├── logo-brand.png
│   │       ├── logo-google.png
│   │       ├── beaver-login.png
│   │       ├── profile-expert-kim.png
│   │       └── ...
│   ├── components/                 # 공통 컴포넌트
│   │   ├── AppLayout.tsx
│   │   ├── Header.tsx
│   │   ├── BottomNavigation.tsx
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── StatusBadge.tsx
│   │   └── Toast.tsx
│   ├── pages/                      # 개별 페이지 컴포넌트
│   │   ├── LoginPage.tsx
│   │   ├── ResultPage.tsx
│   │   ├── ShopPage.tsx
│   │   └── ExpertPage.tsx
│   ├── styles/                     # 공통 테마 및 전역 스타일
│   │   ├── theme.ts
│   │   ├── GlobalStyle.ts
│   │   └── styled-components.d.ts
│   ├── data/                       # TypeScript 데이터베이스
│   │   ├── resultData.ts
│   │   ├── shopData.ts
│   │   └── expertData.ts
│   ├── types/                      # TypeScript 타입 정의
│   │   ├── result.ts
│   │   ├── shop.ts
│   │   └── expert.ts
│   ├── App.tsx                     # React Router 셋업 및 App 조립
│   └── main.tsx                    # 엔트리 포인트
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── 작업계획.md                       # 본 작업계획 파일
```
